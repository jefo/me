---
interface Props {
  text: string;
}
const { text } = Astro.props;
---
<div class="flex items-center gap-3 mb-10 font-mono text-[10px] text-text-muted tracking-[0.12em] uppercase">
  <span>{text}</span>
  <div class="flex-1 h-px bg-border-dim"></div>
</div>
