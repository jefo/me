---
interface Props {
  text: string;
}
const { text } = Astro.props;
---
<div class="flex items-center gap-4 mb-10 font-mono text-xs text-content-muted tracking-[0.12em] uppercase">
  <span>{text}</span>
  <div class="flex-1 h-px bg-border-subtle"></div>
</div>

